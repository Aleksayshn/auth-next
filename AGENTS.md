<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.

<!-- END:nextjs-agent-rules -->

Task:
Implement/refine the attached UI in the current codebase.

Context:

- Attached screenshot is the visual source of truth
- Use existing components and tokens where possible
- Keep changes localized to the auth flow

Requirements:

- Match layout and spacing closely
- Preserve dark glassmorphism styling
- Prevent layout shift between auth states
- Keep inputs dark on typed and autofill states
- Use accessible labels and focus states
- Keep animation subtle and premium

Suggested style direction:
slow group drift
subtle parallax
opacity breathing
animated gradient offset
tiny vertical oscillation
slight scale/rotation on grouped paths

The line itself to feel like energy is moving through it:
animated gradient stops
moving stroke highlight
path shimmer

Process:

- First inspect the current files that control this screen
- Briefly state the plan
- Then implement the changes
